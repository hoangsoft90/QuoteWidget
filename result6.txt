================================================================================
PROMPT: Device QA Gate (Phase 4 — prompt_device_qa.md)
================================================================================
Ngày: 2026-09-05
Trạng thái: ✅ PHẦN AGENT DONE — verdict BLOCKED (chờ human tester Wave 1)

================================================================================
1. PREFILIGHT — PASS
================================================================================

  flutter analyze:  No issues found (0 error, 0 warning)
  flutter test:     138/138 All tests passed
  Dead code check:  source/ không tồn tại
                    widget_config_screen.dart : không tồn tại (deleted)
                    widget_preview.dart       : không tồn tại (deleted)
                    refs trong lib/:           0 (grep rỗng)

  baseline trước session: analyze 0 + 138 test.
  Không có test nào loại bỏ trong session này — số lượng giữ nguyên 138.

================================================================================
2. BLOCKER GẤP — CI CHỈ BUILD TEST ADS, KHÔNG CÓ PRODUCTION ADS BUILD
================================================================================

  Mục đích: MUST case C1/H1 yêu cầu TEST_ADS=false (production ad units).
  Project rule cấm build local → phải có build production ads trên GH Actions.

  Fix: add workflow_dispatch input test_ads (default true) vào
       .github/workflows/build-debug-apk.yml

  Release stepchanged thành:
    flutter build apk --release --no-pub \
      --dart-define=TEST_ADS=${{ inputs.test_ads || 'true' }}

  - Push build → giữ nguyên test ads (fallback true).
  - Dispatch test_ads=false → QA candidate với production units.

  Commit: dbd4481
  File:   .github/workflows/build-debug-apk.yml (workflow_dispatch + dart-define)

================================================================================
3. QA CANDIDATE BUILD — GREEN
================================================================================

  Workflow dispatch với test_ads=false:
    run ID:    33972687792
    commit:    dbd4481
    status:    success
    conclusion: success

  Steps (tất cả xanh):
    Set up job ✅
    Checkout code ✅
    Set up Java 17 ✅
    Set up Flutter ✅
    Flutter & Dart versions ✅
    Install dependencies ✅
    Analyze ✅
    Run tests ✅
    Build debug APK ✅
    Show build output ✅
    Fail if build failed ✅ (skipped — success)
    Upload build log ✅
    Upload APK artifact ✅
    Build release APK ✅
    Show release build output ✅
    Fail if release build failed ✅ (skipped — success)
    Upload release APK ✅

  Artifact: release-apk — 30662801 bytes (~30.6 MB), không expired
  (đồng thời có debug-apk 91.7 MB, build-log 1.0 KB)

  TEST_ADS=false → ad units thật trong build:
    banner     : ca-app-pub-6917313063209470/1409128007
    interstitial: ca-app-pub-6917313063209470/1569899782
    rewarded   : ca-app-pub-6917313063209470/7613467914

  Bản chất case C1/H1: build này chứa unit thật, khác sample
  ca-app-pub-3940256099942544/... — đúng expectation khi khời chạy.

================================================================================
4. RUN SHEET — TẠO XONG (.plan/device_qa_run_sheet.md)
================================================================================

  File (gitignored, chỉ local):
    .plan/device_qa_run_sheet.md

  Metadata bảng:
    Version:           1.0.0+1 (pubspec)
    Commit:            dbd4481 (main)
    Build date:        2026-09-05
    Build type:        Release APK, debug-signed bởi CI
    CI run (QA):       33972687792, success
    TEST_ADS:          false
    Artifact:         release-apk (30.6 MB)
    Local build:       None (rule project)

  MUST cases (ghi rõ):
    Wave 1:  A1–A6
    Wave 2:  B1–B3
    Wave 3:  C1–C4
    Wave 4:  D1–D2, E1–E4
    Wave 5:  F1–F5 (F4 shuffle bag + F5 daily → MUST vì đã ship Phase 2B),
             G1–G2
    Wave 6:  H1–H2
    SHOULD:  I1–I5

  Mỗi case có:
    Pre | Steps | Expected | ☐ PASS/FAIL/N/A | Notes | Triage if FAIL (file nghi ngờ)

  Triage hints chọn file thực tế codebase:
    A2, A3, A4, A5, B1, B2, C1, C2, C3, D1, E1, E2, E3, E4,
    F1, F2, F3, F4, F5, G1, G2, H1, H2 — mỗi case có file suspect.

================================================================================
5. DOCS SYNC — features_final.md UPDATED
================================================================================

  Trước session: nhiều feature Phase 1-2B vẫn khắc "⚠️ Partial".
  Sau session: đồng bộ trạng thái theo code thực tế (grep verification).

  Feature ship (đánh dấu ✅):
    Phase 2A: Favorites, Favorites-only (contentFilter), Search,
              Duplicate Collection, Templates/Empty-state
    Phase 2B: Shuffle Bag, Daily rotation, Auto-rotate (1h/3h/6h),
              Tap action (next/open_collection/open_app/copy),
              Responsive 4×2, Quick Share remember last collection

  Deferred (🚫, ghi rõ để tránh claim ảo):
    Export collection JSON/CSV, App shortcuts, Material You,
    TXT/clipboard import entry

  Dead code (✅):
    widget_config_screen.dart: deleted
    processShareText / ShareResult: deleted
    legacy source/ tree: deleted (canonical = root)

  Backup semantics (✅):
    export: widgetConfigs: [] (không kèm config active)
    import: drop mọi widgetConfigs (const []) — không tạo phantom config

  Reconciliation (✅):
    reconcileWidgetConfigs() full 2-way scan mỗi lần — bỏ early-return fast path
    cleanupOrphanWidgetMappings: merged vào reconcile

================================================================================
6. WORKING LOG
================================================================================

  Đã cập nhật working.md:
    [2026-09-05] prompt_device_qa — Phase 4 Device QA prep
      - Preflight PASS: analyze 0, 138 tests, dead code gone
      - CI: workflow_dispatch test_ads input, commit dbd4481
      - QA candidate build 33972687792 = success, TEST_ADS=false
      - Run sheet: .plan/device_qa_run_sheet.md
      - Docs: features_final.md sync (Phase 1-2B shipped)
      - openspec: device-qa-gate (proposal + tasks)

================================================================================
7. ĐANG DỠE — CHỜ HUMAN TESTER
================================================================================

  Verdict:  BLOCKED
  Lý do:    Agent không có thiết bị thật → không thể run waves.
            Tất cả cues trong run sheet đang ☐ unchecked.

  Next step (human):
    1. Tải app-release.apk từ CI run 33972687792
    2. Cài trên Device A (stock/Pixel) — ghi model + Android version
    3. Chạy Wave 1 (A1 → A2 → A3 → A4 → A5) — hướng dẫn chi tiết trong
       session 직전 (mỗi case có hướng dẫn từng bước UI thật).
    4. Báo kết quả từng case → agent triage FAIL, fix blocker nếu có,
       re-run affected case.
    5. Sau A1–A5 pass → tiếp Wave 2–6 (theo run sheet).
    6. Wave 3 C1/H1: chỉ chạy trên QA candidate build (TEST_ADS=false).

================================================================================
8. QUYẾT ĐỊNH / CÂU HỎI CẦN XEM XÉT TRƯỚC KHI DÒNG SỐ NEXT SESSION
================================================================================

  Q1: Tester có muốn mình tóm tắt lại hướng dẫn từng bước Wave 1
      (A1–A5) vào file riêng, hay chỉ dùng run sheet hiện có?
      (run sheet đã có đủ Preconditions/Steps/Expected + Triage.)

  Q2: Cần mở rộng run sheet thêm các trường thời gian thực thi,
      logcat snippet cố định, hay giữ nguyên cấu trúc checklist hiện tại?

  Q3: Build QA candidate có cần thêm AAB artifact (cho Play Store upload
      sau QA) không, hay chỉ cần APK để test device?

Acknowledge:
  - Chưa commit handoff file (nằm ngoài git track).
  - run sheet (local .plan/) chưa commit — đúng thiết kế.
  - features_final.md sync chưa push (đang chờ gộp vào commit docs).

================================================================================
END
================================================================================
